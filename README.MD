# Практикум по курсу "Введение в Kubernetes"

**Дипломный проект - Задание "Nightmare" (на четверочку)**

## Оглавление

- [Задание](#задание)
- [Описание решения](#описание-решения)
- [Архитектура](#архитектура)
- [Структура проекта](#структура-проекта)
- [Соответствие требованиям задания "Nightmare"](#соответствие-требованиям-задания-nightmare)
- [Технические детали](#технические-детали)
- [Особенности реализации](#особенности-реализации)
- [Тестирование](#тестирование)
- [Платформа](#платформа)
- [Развертывание](#развертывание)
- [Автор](#автор)

---

## Задание

**Задание уровня "Nightmare" (на четверочку)**
Написать конфигурацию для кластера Kubernetes, в котором: 
- Поднять любое веб-приложение, которое использует базу данных для хранения данных.
- Базу данных желательно брать из кластерных (MongoDB, CockroachDB).
- Дополнительно установить в кластер Ingress Controller (любой на выбор, кроме ingress-nginx), создать объект Ingress для доступа к веб-приложению через NodePort.
- Для хранения данных использовать Volume (emptyDir).

**Примечания к заданию:** 

- Локация репозитория не имеет значения: GitFlic, GitHub, где больше нравится.
- Главное - чтобы можно было забрать проект командой git clone.
- Наличие файла README.md в репозитории - обязательно:
- Вы должны дать минимальное описание проекта.
- Требования к репозиторию: все манифесты проекта должны быть в директории репозитория deploy, чтобы команда втаскивала сразу весь проект:
```
kubectl apply -f deploy/
```

Описание платформы для развертывания решения:
- ``Kubernetes`` версии ``1.34``,
- подключение к интернету есть,
- доступ к ``Docker Hub`` есть, образы оттуда можно использовать.
- ``IngressController`` никакой не установлен, доступен ``StorageClass`` с идентификатором ``csi-driver-lvm-linear``

**Т.е. решение должно закатываться на наш рабочий кластер в учебном классе.**

**Сроки сдачи:**
- до 10 декабря (ровно до 23:59) нужно прислать любую версию, даже с ошибками. 
Если до 10 декабря ничего не прислали, то срок вышел.
- До 12 декабря (аналогично до 23:59) прислать финальную версию.
Время учитывается строго, без опозданий, пожалуйста.

## Описание решения

Проект представляет собой веб-приложение с базой данных MongoDB, 
развернутое в Kubernetes кластере с использованием Ingress Controller для доступа извне.

## Архитектура

**Компоненты решения:**

1. **MongoDB** - кластерная база данных для хранения данных
   - Образ: `mongo:7.0`
   - Replicas: 1
   - Хранилище: emptyDir (временное, как требуется в задании)
   - Порт: 27017
   - Service: ClusterIP

2. **Веб-приложение** - Mongo Express (веб-интерфейс для работы с MongoDB)
   - Образ: `mongo-express:1.0.2`
   - Replicas: 2 (для отказоустойчивости)
   - Порт: 8081 (внутренний), 80 (Service)
   - Service: ClusterIP

3. **Ingress Controller** - Traefik v2.10
   - Выбран Traefik (не ingress-nginx, как требуется в задании)
   - Replicas: 2
   - Доступ: NodePort 30080 (HTTP), 30081 (Dashboard)
   - Namespace: kube-system

4. **Ingress** - правила маршрутизации
   - Host: webapp.local
   - Path: /
   - Backend: webapp service (port 80)

## Структура проекта

```
ga_kubernetes/
├── deploy/                          # Директория с манифестами
│   ├── 01-namespace.yaml           # Namespace для приложения
│   ├── 02-mongodb-deployment.yaml  # Deployment MongoDB
│   ├── 03-mongodb-service.yaml     # Service для MongoDB
│   ├── 04-webapp-deployment.yaml   # Deployment веб-приложения
│   ├── 05-webapp-service.yaml      # Service для веб-приложения
│   ├── 06-traefik-rbac.yaml        # RBAC для Traefik
│   ├── 07-traefik-deployment.yaml  # Deployment Traefik
│   ├── 08-traefik-service.yaml     # Service для Traefik (NodePort)
│   ├── 09-ingressclass.yaml        # IngressClass для Traefik
│   └── 10-webapp-ingress.yaml      # Ingress для веб-приложения
├── README.MD                        # Описание решения (этот файл)
└── DEPLOY.MD                        # Инструкции по развертыванию

```

## Соответствие требованиям задания "Nightmare"

✅ **Веб-приложение с базой данных** - Mongo Express + MongoDB  
✅ **Кластерная база данных** - MongoDB (поддерживает кластеризацию)  
✅ **Ingress Controller** - Traefik (не ingress-nginx)  
✅ **Доступ через Ingress с NodePort** - NodePort 30080  
✅ **Хранилище emptyDir** - используется для MongoDB  

## Технические детали

**Namespace:** `webapp` - для изоляции приложения  
**Ingress Controller Namespace:** `kube-system` 
- стандартное расположение системных компонентов

**Порты:**
- NodePort 30080 - HTTP доступ к приложению через Traefik
- NodePort 30081 - Traefik Dashboard (для мониторинга)

**Credentials:**
- MongoDB: admin / password123
- Mongo Express UI: admin / pass

## Особенности реализации

1. **emptyDir для MongoDB** - данные хранятся временно на ноде, 
при удалении Pod данные теряются (как требуется в задании)
2. **Traefik как Ingress Controller** - современный, 
легковесный контроллер с поддержкой динамической конфигурации
3. **Mongo Express** - удобный веб-интерфейс для демонстрации работы с MongoDB
4. **2 реплики веб-приложения** - для демонстрации балансировки нагрузки через Ingress
5. **RBAC настроен** - ServiceAccount, ClusterRole и ClusterRoleBinding для Traefik

## Тестирование

После развертывания приложение доступно по адресу:
- **HTTP:** `http://<NODE_IP>:30080` с заголовком `Host: webapp.local`
- **Traefik Dashboard:** `http://<NODE_IP>:30081`

Пример команды для тестирования:
```bash
curl -H "Host: webapp.local" http://<NODE_IP>:30080
```

Или добавляем в `/etc/hosts` (Linux/Mac) или `C:\Windows\System32\drivers\etc\hosts` (Windows):
```
<NODE_IP> webapp.local
```

И открываем в браузере: `http://webapp.local:30080`

## Платформа

Решение разработано для:
- **Kubernetes:** v1.34
- **StorageClass:** csi-driver-lvm-linear (доступен, но не используется в этом задании)
- **Доступ к Docker Hub:** есть
- **Интернет:** есть

## Развертывание

Подробные пошаговые инструкции по развертыванию приложения в кластере Kubernetes:

📖 **[Инструкция по развертыванию (стандартный кластер) (DEPLOY.MD)](DEPLOY.MD)**  
📖 **[Инструкция по развертыванию (WSL + Minikube) (DEPLOY.WSL.MD)](DEPLOY.WSL.MD)**  
📖 **[Правила для Warp AI (WARP.md)](WARP.md)**

**Быстрый старт:**
```bash
# Применяем все манифесты
kubectl apply -f deploy/

# Проверяем статус
kubectl get all -n webapp
kubectl get ingress -n webapp
```

Полный список команд для копирования:
📋 **[Все команды для развертывания (cmds.sh)](cmds.sh)**

## Автор

Иконников Михаил <mishaikon@gmail.com>
